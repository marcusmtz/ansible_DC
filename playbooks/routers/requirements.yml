---
collections:
  - name: ansible.netcommon
    version: ">=5.0.0,<7.0.0"

  - name: ansible.utils
    version: ">=2.10.0,<5.0.0"

  - name: community.general
    version: ">=7.0.0,<10.0.0"

  - name: pfsensible.core
    version: ">=0.5.0,<1.0.0"
