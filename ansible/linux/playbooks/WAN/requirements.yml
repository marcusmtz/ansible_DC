---
# Colecciones Ansible requeridas para configurar pfSense

collections:
  # Dependencias requeridas por netgate.pfsense
  - name: ansible.netcommon
    version: ">=5.0.0"
  
  - name: ansible.utils
    version: ">=2.10.0"
  
  # Colección OFICIAL de Netgate para pfSense
  - name: netgate.pfsense
    version: ">=0.4.0"
